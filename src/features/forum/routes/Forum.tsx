import { Container } from 'react-bootstrap';
import { ForumView } from '../components/ForumView';

export const Forum = () => {
    return (
        <Container>
            <ForumView />
        </Container>
    );
};
